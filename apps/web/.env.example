# Copy this file to .env and adjust if the api runs somewhere other than the default.
# Not a secret — just the api's base URL, kept out of git so each machine can point
# at a different port/host without committing local config.
VITE_API_BASE_URL="http://localhost:3000"

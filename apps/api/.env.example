# Copy this file to .env and adjust if needed. The default is a local SQLite file
# next to this package — not a secret, just kept out of git so each clone/machine
# can have its own local dev.db without committing generated data.
DATABASE_URL="file:./dev.db"

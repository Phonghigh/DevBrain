# Progress log

Append-only. Newest at the bottom. One section per completed task.
Written by the routine loop (see [ROUTINE.md](ROUTINE.md) step 7).

Format:

```
## <YYYY-MM-DD> — <task-id> <title>
- changed: <what>
- files: <key paths>
- verify: <typecheck/lint/test result>
- follow-up: <anything noticed, or none>
- commit: <short message>
```

---

<!-- entries below -->

## 2026-07-14 — DB0-01 .editorconfig + refresh root README.md
- changed: added `.editorconfig` (UTF-8, LF, final newline, trim trailing whitespace, 2-space indent; markdown exempted from trailing-whitespace trimming so hard line breaks survive). Rewrote `README.md` in English: what/why, a Stack line, and a "How it builds itself" section pointing at `tasks/README.md` + the `/loop` one-liner.
- files: `.editorconfig`, `README.md`
- verify: config/docs only — no build tooling exists yet (pnpm workspace lands in DB0-02), so nothing to typecheck/lint/test. Confirmed both files present and correct; touches neither `apps/**` nor `packages/**`, so no learn-log report required (Stop hook doesn't ask).
- follow-up: none. Next eligible: DB0-02 (pnpm workspace).
- commit: tasks(DB0-01): editorconfig + english readme

## 2026-07-14 — DB0-02 pnpm workspace
- changed: turned the repo into a pnpm monorepo. `pnpm-workspace.yaml` globs `apps/*` + `packages/*`; root `package.json` (`private`, `packageManager: pnpm@11.9.0`, `engines.node >=20`, passthrough scripts build/dev/lint/test/typecheck delegating to `pnpm -r`); `.npmrc` (`auto-install-peers`, non-strict peer deps). Also added `.gitattributes` (`* text=auto eol=lf`) to enforce LF and clear the CRLF/`.editorconfig` mismatch Git warned about on DB0-01.
- files: `pnpm-workspace.yaml`, `package.json`, `.npmrc`, `.gitattributes`, `pnpm-lock.yaml` (generated)
- verify: `pnpm install` → "Already up to date", exit 0, using pnpm v11.9.0 (matches the pinned version, no corepack issue); `pnpm-lock.yaml` generated; `node_modules/` correctly git-ignored. Root scripts are no-ops until packages exist (DB0-05+). Config only — touches neither `apps/**` nor `packages/**`, so no learn-log required.
- follow-up: `.gitattributes` bundled here (one line, foundational repo config) rather than as its own task. pnpm noted 11.9.0 → 11.13.0 available; left pinned intentionally for reproducibility. Next eligible: DB0-03 (shared TS config).
- commit: tasks(DB0-02): pnpm workspace

## 2026-07-14 — DB0-03 Shared TS base config
- changed: added `tsconfig.base.json` — strict (+ `noUncheckedIndexedAccess`, `noImplicitOverride`, `noFallthroughCasesInSwitch`), ES2022 target/lib, Bundler/ESM flavor (`module ESNext` + `moduleResolution Bundler` + `verbatimModuleSyntax` + `isolatedModules`), `declaration`/`declarationMap`/`sourceMap`, `skipLibCheck`. Added solution-style root `tsconfig.json` (`files: []`, `references: []` — packages register themselves later). Added `typescript@^5.9.2` (resolved 5.9.3) as a root devDependency so `tsc` can run.
- files: `tsconfig.base.json`, `tsconfig.json`, `package.json` (+ devDep), `pnpm-lock.yaml` (generated)
- verify: created a throwaway `_tsprobe/` package extending the base with a strict source file, ran `pnpm exec tsc -p _tsprobe/tsconfig.json --noEmit` → passes (exit 0), then removed the probe. Config only — touches neither `apps/**` nor `packages/**`, so no learn-log required.
- follow-up: base is deliberately Bundler/ESM; `apps/api` (NestJS) will override `module=CommonJS` + `emitDecoratorMetadata`/`experimentalDecorators` + `verbatimModuleSyntax=false` in its own tsconfig (noted in the base file's comment). typescript pinned to a caret 5.9 range (5.9.3 installed; 7.0.2 available but not adopted). Next eligible: DB0-04 (ESLint + Prettier + husky).
- commit: tasks(DB0-03): shared ts base config

## 2026-07-14 — DB0-04 ESLint + Prettier + lint-staged + husky
- changed: added flat ESLint v9 config (`eslint.config.mjs`) — `@eslint/js` + `typescript-eslint` recommended (non-type-checked, no tsconfig project needed), `eslint-config-prettier` last so Prettier owns formatting; ignores dist/build/coverage/node_modules. Prettier config (`.prettierrc`: semi, single quotes, 100 cols, trailing-comma all, LF) + `.prettierignore` (build/deps/lockfile + all `*.md`, so hand-authored docs aren't reflowed). husky v9 (`prepare: husky`) + `.husky/pre-commit` running `pnpm exec lint-staged`; lint-staged runs `eslint --fix` + `prettier --write` on staged code, `prettier --write` on staged json/yaml/css/html (md excluded, matching .prettierignore). Root scripts: `lint:root` (`eslint .`), `format`, `format:check`, `prepare`.
- files: `eslint.config.mjs`, `.prettierrc`, `.prettierignore`, `.husky/pre-commit`, `package.json` (devDeps + scripts + lint-staged block), `pnpm-lock.yaml` (generated)
- verify: `pnpm install` OK (husky prepare set `core.hooksPath=.husky/_`); `pnpm exec eslint .` → exit 0 (clean); staged a deliberately mis-formatted `_fmtprobe.ts` (`export const x=1`) and ran `pnpm exec lint-staged` → auto-fixed to `export const x = 1;`, then discarded the probe. Config only — touches neither `apps/**` nor `packages/**`, so no learn-log required.
- follow-up: root `lint` still delegates to `pnpm -r lint` (no-op until packages define a lint script, starting DB0-05); direct repo linting is `pnpm lint:root`. Newer majors available (eslint 10, lint-staged 17) but left on stable current majors. Next eligible: DB0-05 (scaffold packages/shared — first code task, ships the first learn-log lesson).
- commit: tasks(DB0-04): eslint + prettier + lint-staged + husky

## 2026-07-14 — DB0-05 Scaffold packages/shared
- changed: created `packages/shared` (`@devbrain/shared`), the first real code package: `package.json` (ESM `type: module`, `exports` map for `.` -> `./dist/index.js`, `build`/`typecheck`/`lint`/`test` scripts matching the root passthrough names), `tsconfig.json` (extends the base config, `composite: true` for project references, `outDir: dist`, excludes `src/__tests__` from the build), `src/index.ts` (first real export: `LINT_LIMITS` — the copy-overlap % and max-word thresholds from spec §6.3), `vitest.config.ts` (Node environment, no DOM), and `src/__tests__/smoke.test.ts` asserting the two threshold values. Wired the root `tsconfig.json`'s `references` array to `./packages/shared`.
- files: `packages/shared/{package.json,tsconfig.json,vitest.config.ts,src/index.ts,src/__tests__/smoke.test.ts}`, `tsconfig.json`, `pnpm-workspace.yaml` (approved the `esbuild` build script), `pnpm-lock.yaml` (generated)
- verify: `pnpm --filter @devbrain/shared build` -> compiles cleanly, `dist/` contains only `index.*` (no leaked test files after excluding `src/__tests__`); `test` -> 2 passed; `typecheck` (both package-level and root `tsc -b tsconfig.json` via project references) -> clean; `pnpm exec eslint packages/shared` -> clean.
- follow-up: hit `ERR_PNPM_IGNORED_BUILDS` (pnpm blocking esbuild's postinstall script) — approved it explicitly in `pnpm-workspace.yaml` with a comment explaining why (well-known package, script just downloads its binary). First learn-log report written: [docs/learn-log/DB0-05-scaffold-shared.md](../docs/learn-log/DB0-05-scaffold-shared.md). Next eligible: DB0-06 (scaffold apps/api).
- commit: tasks(DB0-05): scaffold packages/shared

## 2026-07-14 — DB0-06 Scaffold apps/api (NestJS + /health)
- changed: created `apps/api` (name `api`, matches `pnpm --filter api`): a NestJS 11 API skeleton, hand-written (not via `nest new`, same approach as DB0-05's `packages/shared`). `AppModule` → `HealthModule` → `HealthController` (`@Controller('health')` + `@Get()`) returning `{status:'ok'}`. `apps/api/tsconfig.json` overrides the shared ESM/Bundler base with CommonJS + `experimentalDecorators`/`emitDecoratorMetadata` (required for NestJS's decorator-based DI) + `moduleResolution: Node10`. Uses `@nestjs/cli` (`nest build`/`nest start --watch`) rather than a fast/esbuild-based runner, since those skip decorator-metadata emission that Nest's DI needs.
- files: `apps/api/{package.json,tsconfig.json,nest-cli.json,src/main.ts,src/app.module.ts,src/health/{health.module,health.controller}.ts}`, `pnpm-lock.yaml` (generated)
- verify: `pnpm --filter api typecheck` clean (after adding `@types/node`, see follow-up); `pnpm --filter api build` → `dist/` produced; booted the compiled server directly (`node dist/main.js`) and curled it for real: `curl -i http://localhost:3000/health` → `HTTP/1.1 200 OK` `{"status":"ok"}`, boot log showed `AppModule`/`HealthModule` dependencies initialized + `Mapped {/health, GET} route`; `pnpm exec eslint apps/api` clean; root `pnpm build`/`typecheck`/`lint` (via `pnpm -r`) all pick up `apps/api` automatically alongside `packages/shared`.
- follow-up: hit a real typecheck error — `Cannot find name 'process'` in `main.ts` — fixed by adding `@types/node` as a devDependency (any package touching Node built-ins needs it; `packages/shared` didn't need it since `LINT_LIMITS` never touches Node APIs). Second learn-log report written: [docs/learn-log/DB0-06-scaffold-api.md](../docs/learn-log/DB0-06-scaffold-api.md). Next eligible: DB0-07 (Prisma in apps/api).
- commit: tasks(DB0-06): scaffold apps/api

## 2026-07-14 — DB0-07 Prisma in apps/api
- changed: wired Prisma + SQLite into `apps/api`. Ran `prisma init --datasource-provider sqlite`; the installed Prisma version (7.8.0) turned out to use a materially different architecture than the task notes assumed — no inline `url` in `schema.prisma` anymore, a separate `prisma.config.ts` (needs `dotenv`), and the client now requires an explicit driver adapter (`@prisma/adapter-better-sqlite3` + `better-sqlite3`) rather than reading a schema-baked URL. Added `PrismaService` (extends the generated `PrismaClient`, constructs it with the adapter, implements `onModuleInit`/`onModuleDestroy` for connect/disconnect) and `@Global()` `PrismaModule` (exports `PrismaService`), imported into `AppModule`. Added `import 'dotenv/config'` to `main.ts` so the running app (not just the CLI) loads `DATABASE_URL`. Added a `postinstall: "prisma generate"` script so the git-ignored generated client regenerates automatically on fresh installs. Cleaned up: removed the redundant auto-created `apps/api/.gitignore` (root `.gitignore` already covers `.env`/`*.db`; added `apps/api/src/generated/` there instead), added `apps/api/.env.example`, trimmed an irrelevant promotional comment from `schema.prisma`.
- files: `apps/api/{package.json,tsconfig.json,nest-cli.json,.env.example,prisma.config.ts,prisma/schema.prisma,src/main.ts,src/app.module.ts,src/prisma/{prisma.module,prisma.service}.ts}`, `.gitignore`, `pnpm-workspace.yaml` (approved `@prisma/engines`/`prisma`/`better-sqlite3` build scripts), `pnpm-lock.yaml` (generated)
- verify: `pnpm --filter api typecheck`/`build` clean; `prisma migrate dev --name init-empty` → created `dev.db`, correctly reported "already in sync" (schema has zero models by design — real models land in DB1-01); simulated a fresh clone (deleted `src/generated/` + `dev.db`, ran plain `pnpm install`) and confirmed `postinstall` regenerated the client automatically; booted the real compiled server and confirmed the boot log shows `PrismaModule dependencies initialized` (proves DI + `$connect()` succeeded) with `dev.db` present on disk, `/health` still 200 OK; root `pnpm build`/`typecheck`/`lint` all green across all 3 workspace packages.
- follow-up: hit and fixed two real bugs beyond the Prisma version mismatch — (1) a stale `.tsbuildinfo` (from `"incremental": true`, shared between the `build` and `typecheck` scripts) made `tsc` silently skip emitting after `dist/` was manually deleted; removed `incremental` from `apps/api/tsconfig.json` entirely to close off this class of bug. (2) On this Git Bash/Windows environment, bash's `$!` PID didn't match the real OS process for a backgrounded `node` process, leaving a zombie server holding port 3000 for a while (from the DB0-06 boot test) — killed it via the real PID read from the process's own boot log, and used that method for all subsequent boot tests. Third learn-log report written: [docs/learn-log/DB0-07-prisma-in-api.md](../docs/learn-log/DB0-07-prisma-in-api.md). Next eligible: DB0-08 (scaffold apps/web — its dep DB0-05 was already satisfied, so it's next despite DB1-01 also now being unblocked, per file order).
- commit: tasks(DB0-07): prisma in apps/api

## 2026-07-16 — DB0-08 Scaffold apps/web (Vite + React + router)
- changed: created `apps/web` (name `web`, matches `pnpm --filter web`) — the first front end, hand-written rather than generated by `npm create vite` (same approach as DB0-05/DB0-06; the generator's demo counter/CSS/logos would all be deleted anyway). Vite 7 + React 19 + react-router 7. `main.tsx` mounts `AppRoutes` in `BrowserRouter` + `StrictMode`; `app/router.tsx` holds the route table **without** its own Router provider (so DB0-10 can mount the same table in a `MemoryRouter`) — an `AppLayout` (NavLink nav + `<Outlet/>`) wrapping `/inbox` `/distill` `/browse`, with `index` → `<Navigate to="/inbox" replace/>`. `DistillRoute` imports `LINT_LIMITS` from `@devbrain/shared` and renders the real numbers, proving the workspace wire. `tsconfig.json` extends the shared base as-is (unlike api, no override needed — the base's ESM/Bundler flavor suits Vite) adding DOM libs, `jsx: react-jsx`, `noEmit` (Vite compiles; tsc only checks).
- files: `apps/web/{package.json,tsconfig.json,vite.config.ts,index.html,src/main.tsx,src/app/router.tsx,src/app/routes/{Inbox,Distill,Browse}Route.tsx}`, `pnpm-lock.yaml` (generated)
- verify: booted the real dev server (`vite v7.3.6 ready in 580ms`) and drove it with **headless Edge** — `/` renders the Inbox heading (redirect works), `/inbox` `/distill` `/browse` each render their own `<main>`, and Distill shows "above 35% overlap ... past 200 words" read live from `packages/shared`. Also rendered all 4 routes through a throwaway Node SSR probe (esbuild + `renderToStaticMarkup` + `StaticRouter`), confirming NavLink active states; probe deleted after. From a **fully clean state** (all `dist/` + `.tsbuildinfo` deleted): root `pnpm -w typecheck` / `lint` / `test` / `build` / `format:check` all green across all 3 packages, with `-r` topological ordering building `shared` before `web`.
- follow-up: hit a real gap — web's `typecheck` initially passed only because `packages/shared/dist` already existed from an earlier task; on a fresh clone it fails with `TS2307: Cannot find module '@devbrain/shared'`, because the exports map points at `dist/` and TS resolution fails there *before* any project-reference redirect can apply (my first comment in `tsconfig.json` claimed otherwise — it was wrong and is now corrected). Fixed by declaring `"references": [{ "path": "../../packages/shared" }]` and switching the script from `tsc --noEmit` to **`tsc -b`**, which builds the referenced project on demand; verified by deleting `dist/` and re-running. Rejected alternatives: requiring a manual `pnpm build` first (breaks fresh clones + CI in DB0-11), and pointing shared's exports at `src/` (would break the CommonJS `apps/api` consumer). Also hit the stale `.tsbuildinfo` trap for the **second task running** (it made `tsc -b` refuse to rebuild `shared` — same class as DB0-07's bug); and burned three attempts proving the pages render (curl 200s only prove the history fallback served the shell; headless Edge `--dump-dom` returned zero bytes until switched to `Start-Process -RedirectStandardOutput`). Fourth learn-log report written: [docs/learn-log/DB0-08-scaffold-web.md](../docs/learn-log/DB0-08-scaffold-web.md). Note `apps/web` is deliberately **not** added to the root `tsconfig.json` `references` (a solution-referenced project must be `composite`, which conflicts with web's `noEmit`) — same as `apps/api`. Next eligible: DB0-09 (vitest + supertest in apps/api).
- commit: tasks(DB0-08): scaffold apps/web

## 2026-07-16 — DB0-09 Vitest + supertest in apps/api (first e2e)
- changed: gave `apps/api` a test harness + its first e2e. Picked **vitest** over Nest's default jest (the task asked for a choice + rationale): `packages/shared` already uses vitest, so this keeps one runner/config style repo-wide rather than two. `test/health.e2e-spec.ts` boots the whole app via `Test.createTestingModule({imports:[AppModule]})` → `createNestApplication()` → `app.init()` in `beforeAll`, `app.close()` in `afterAll`, and asserts `GET /health` → 200 `{status:'ok'}` through supertest (fake HTTP, no real port — deliberately unlike DB0-06/DB0-07's manual boot-and-curl, which left a zombie process on port 3000). Added `unplugin-swc` + `@swc/core` and wired `swc.vite({ jsc: { parser: { decorators: true }, transform: { legacyDecorator: true, decoratorMetadata: true } } })` into `vitest.config.ts` — see follow-up, this is the whole story of the task. Approved `@swc/core`'s install script in `pnpm-workspace.yaml` with a comment (same gate DB0-05 hit with esbuild). Added `"test": "vitest run"` matching the root passthrough.
- files: `apps/api/{package.json,vitest.config.ts,test/health.e2e-spec.ts}`, `pnpm-workspace.yaml`, `pnpm-lock.yaml` (generated)
- verify: `pnpm --filter api test` → 1 file / 1 test passed (~1s). From a fully clean state (all `dist/` + `.tsbuildinfo` deleted): root `pnpm -w typecheck` / `lint` / `test` / `build` / `format:check` all green across all 3 packages. Confirmed `nest build`'s `dist/` contains **no** test or probe files (tsconfig `include` is `src/**/*` only). Also simulated CI by moving `.env` **and** `dev.db` away and re-running: still green (PrismaService falls back to `file:./dev.db` and better-sqlite3 creates an empty, git-ignored file), so DB0-11 needs no secrets or DB setup step; restored both afterwards.
- follow-up: the real find — **the first version of this test passed while the setup was silently broken.** Vitest transpiles with esbuild, which cannot emit decorator metadata (`design:paramtypes`), the thing NestJS's DI reads to know what to inject. Nothing in `apps/api` injects anything yet (`HealthController`'s constructor is empty), so the suite was green and would have stayed green until DB1-03. Proved it with a throwaway probe controller mimicking DB1-03's `CapturesController` (`constructor(private prisma: PrismaService)`): under esbuild, `paramtypes: undefined` and Nest **did not throw** — it silently constructed the controller with no args, leaving `this.prisma === undefined`. Fixed with SWC; probe deleted after. Second-order lesson worth recording: after adding SWC the probe still reported `injected:false`, which looked like SWC had changed nothing — but the fuller probe output (`typeofPrisma:"object"`, `ctorName:"PrismaService"`, `hasConnect:"function"`) showed injection working and my `instanceof` assertion was the broken part (unreliable under Vite's SSR transform; not fully root-caused, flagged as such in the report rather than explained away). Re-running the **esbuild baseline with the same detailed probe** is what settled it. Known gap, deliberately not expanded into this task: `test/` isn't covered by `pnpm -w typecheck` (tsconfig `include` is `src/**/*`), consistent with `packages/shared` excluding `src/__tests__` — type errors in tests surface only at run time. Fifth learn-log report written: [docs/learn-log/DB0-09-vitest-supertest-api.md](../docs/learn-log/DB0-09-vitest-supertest-api.md). Next eligible: DB0-10 (vitest + Testing Library in apps/web).
- commit: tasks(DB0-09): vitest + supertest in apps/api

## 2026-07-16 — DB0-10 Vitest + Testing Library in apps/web (first render test)
- changed: gave `apps/web` a render-test harness + its first test, retiring DB0-08's manual verification ceremony (boot a dev server, drive headless Edge) in favour of something that runs in 0.4s on every `pnpm -w test`. Deps: vitest (already the repo runner), `jsdom`, `@testing-library/react`, `@testing-library/jest-dom`. Test config lives in **`vite.config.ts`** (not a separate `vitest.config.ts` like `apps/api`) because a standalone vitest config *replaces* rather than merges — web would lose `@vitejs/plugin-react` and render without the JSX transform; the api has no plugin to lose, hence the difference. `src/test/setup.ts` registers jest-dom's matchers (+ their types) and an `afterEach(cleanup)` so jsdom's shared document doesn't leak between tests. `src/app/routes/InboxRoute.test.tsx` renders `InboxRoute` in a `MemoryRouter` and asserts `getByRole('heading', { name: 'Inbox' })` — DB0-08's choice to keep `router.tsx` free of its own Router provider is what makes this trivial. Added `"test": "vitest run"` matching the root passthrough.
- files: `apps/web/{package.json,vite.config.ts,src/test/setup.ts,src/app/routes/InboxRoute.test.tsx}`, `pnpm-lock.yaml` (generated)
- verify: `pnpm --filter web test` → 1 file / 1 test passed. **Mutation-checked the test rather than trusting a first-run pass** (per DB0-09's lesson): temporarily downgraded `<h1>Inbox</h1>` → `<p>Inbox</p>` (same text, no longer a heading) and confirmed it fails with `TestingLibraryElementError: Unable to find an accessible element with the role "heading" and name "Inbox"` — proving the role query catches an accessibility regression a text search would miss; restored the file and confirmed via `git status` it was byte-identical to the committed version. From a fully clean state (all `dist/` + `.tsbuildinfo` deleted): root `pnpm -w typecheck` / `lint` / `test` / `build` / `format:check` all green — **all 3 packages now have tests (4 total: shared 2, api 1, web 1)**. Confirmed no test/setup files leak into `apps/web/dist` (vite only bundles what's reachable from `index.html`).
- follow-up: no snags — the harness worked first try; the effort went into proving the test can fail. Scope note: installed `@testing-library/user-event` reflexively then **removed it** — nothing in this task types or clicks; it earns its place in DB2-08 (Distill editor). Deliberately **not** done here, filed as a follow-up instead of widening the commit: the route *table* from DB0-08 (the `/` → `/inbox` redirect and route mapping) is still only verified by hand in a browser — a `MemoryRouter` test over `AppRoutes` with `initialEntries` would automate it cheaply, and is worth doing when DB1-06 touches the Inbox screen. Unlike `apps/api`, web's tests **are** covered by `pnpm -w typecheck` (its tsconfig `include` is `src`, which contains the tests) — so the DB0-09 gap doesn't apply here. Sixth learn-log report written: [docs/learn-log/DB0-10-vitest-testing-library-web.md](../docs/learn-log/DB0-10-vitest-testing-library-web.md). Next eligible: DB0-11 (GitHub Actions CI — the last Phase 0 task; its deps DB0-04/05/06/08 are all done).
- commit: tasks(DB0-10): vitest + testing library in apps/web

## 2026-07-16 — DB0-11 GitHub Actions CI
- changed: added `.github/workflows/ci.yml` — one `verify` job on `ubuntu-latest`: `actions/checkout@v4` → `pnpm/action-setup@v4` (no version pinned; it reads the root `packageManager: pnpm@11.9.0`, so CI's pnpm can't drift from local) → `actions/setup-node@v4` (Node 20, `cache: pnpm`) → `pnpm install --frozen-lockfile` → `pnpm typecheck` → `pnpm lint` → `pnpm test` → `pnpm build`. Each step maps 1:1 to a root script, so CI is reproducible locally. Triggers on push to `main` + all PRs, with a `concurrency` group keyed on workflow+ref and `cancel-in-progress: true` (a new push cancels the previous in-flight run rather than queueing).
- files: `.github/workflows/ci.yml`
- verify: can't run Actions locally, so validated everything that *is* checkable. (1) Parsed the YAML with the `yaml` library and asserted the structure programmatically: one `verify` job, `runs-on: ubuntu-latest`, concurrency + cancel-in-progress present, steps in the right order. (2) Confirmed all 4 referenced root scripts exist (`typecheck`/`lint`/`test`/`build` → `pnpm -r <script>`). (3) `pnpm install --frozen-lockfile` → "Already up to date", so the committed lockfile is in sync and CI won't fail on it. (4) **Simulated the CI environment end-to-end**: moved `.env` and `dev.db` away, deleted `src/generated/` + all `dist/` + all `.tsbuildinfo`, then ran `prisma generate` (the api's postinstall — works with no `DATABASE_URL`, exit 0) followed by typecheck → lint → test → build: **all four PASS with no `.env` and no database**, confirming CI needs no secrets and no DB setup step. Restored `.env`/`dev.db` afterwards and confirmed a clean `git status`. (5) `prettier --check` clean on the workflow; confirmed `.github/` isn't git-ignored.
- follow-up: **Phase 0 is complete (11/11).** Next eligible is DB1-01 (Prisma schema `Capture`/`Concept`/`Link` + first migration) — the start of Phase 1 and the first real data-model work. Two things for the owner to decide, deliberately not actioned here: (1) **CI has never actually run** — there are now 4 unpushed commits (DB0-08..DB0-11) and ROUTINE step 7 says don't push, so the workflow's real-world green is still unproven; the logic is validated locally but the first push may still surface something only Actions can show (e.g. `better-sqlite3` compiling on Ubuntu — it has prebuilds, but that path is untested here). (2) **Node 20 is now EOL** (end-of-life April 2026; today is 2026-07-16). The spec called for Node 20 and it's the floor declared in root `engines`, so I followed it — but note vite/jsdom/prisma all require `^20.19` specifically, so the margin is thin, and an EOL runtime gets no security updates. Worth deciding whether to bump `engines` + CI to Node 22 or 24 (local dev is 24). Config only — touches neither `apps/**` nor `packages/**`, so no learn-log required (spec says so explicitly).
- commit: tasks(DB0-11): github actions ci

## 2026-07-18 — DB1-01 Prisma schema Capture/Concept/Link + first migration
- changed: copied the 3 models from spec §5 verbatim into `apps/api/prisma/schema.prisma` (`Capture`, `Concept`, `Link`), removing the stale "no models yet" header comment left by DB0-07. Ran `prisma migrate dev --name capture-concept-link`, which created and applied `prisma/migrations/20260718015621_capture_concept_link/migration.sql` — 3 `CREATE TABLE`s, the `Concept_sourceCaptureId_fkey` foreign key, and both unique indexes (`Concept.slug`, `(fromConceptId, toSlug)` on `Link`).
- files: `apps/api/prisma/schema.prisma`, `apps/api/prisma/migrations/20260718015621_capture_concept_link/migration.sql`
- verify: from a clean state, root `pnpm -w typecheck`/`lint`/`test` all green across all 3 packages. Beyond that, wrote a throwaway e2e test (`test/prisma-probe.e2e-spec.ts`, deleted after) that created a real `Capture` → `Concept` → `Link` chain and ran the exact backlink query pattern spec §5 describes (`Link.where(toSlug=X)` + join `fromConcept`) — passed, proving the schema and typed client work at runtime, not just that a migration file exists. Confirmed via `git status` the repo is clean except the schema + migration files (generated client is git-ignored, as set up in DB0-07).
- follow-up: hit and fixed a real bug — `prisma migrate dev` applied the migration to the database successfully but did **not** regenerate the typed client (`src/generated/prisma/internal/class.ts` still embedded the old, empty schema; `runtimeDataModel: {models: {}}`). `tsc` didn't catch it because nothing yet imports the new model types. Fixed by running `pnpm exec prisma generate` explicitly and re-verifying the generated file now embeds `Capture`/`Concept`/`Link`. Same family of bug as DB0-07/DB0-08's `.tsbuildinfo` staleness — a tool's success message proves its own step worked, not that every downstream artifact is current. Seventh learn-log report written: [docs/learn-log/DB1-01-prisma-schema-capture-concept-link.md](../docs/learn-log/DB1-01-prisma-schema-capture-concept-link.md). Next eligible: DB1-02 (Shared Capture DTOs).
- commit: tasks(DB1-01): prisma schema capture/concept/link

## 2026-07-18 — DB1-02 Shared Capture DTOs
- changed: added `CaptureStatus`/`CreateCaptureDto`/`CaptureDto` to `packages/shared/src/capture.ts` (re-exported from `index.ts`). Added `@devbrain/shared` as a `workspace:*` dependency of `apps/api` (its first-ever import of the shared package) and `apps/api/src/captures/capture.mapper.ts` (`toCaptureDto`) — the first real consumer, converting a Prisma `Capture` row into the DTO (Date → ISO string). Wrote `tasks/backlog/phase-1.md` (no P1 detailed specs existed yet) with DB1-02's spec.
- files: `packages/shared/src/{capture.ts,index.ts,__tests__/capture.test.ts}`, `apps/api/{package.json,tsconfig.json,src/captures/{capture.mapper.ts,capture.mapper.spec.ts}}`, `pnpm-lock.yaml` (generated), `tasks/backlog/phase-1.md`
- verify: hit and fixed a real fresh-clone bug (see follow-up) using the same project-reference + `tsc -b` fix DB0-08 already validated for `apps/web`. After fixing: full clean-state verification — deleted every `dist/` and `.tsbuildinfo` in the repo, then ran `pnpm -w typecheck` / `lint` / `test` / `build` in CI order, all green across all 4 packages (typecheck now also builds `packages/shared`'s `dist/` as a side effect, proving the reference works). Confirmed no `.spec.js` leaked into `apps/api/dist/` (added `src/**/*.spec.ts` to its tsconfig `exclude`, matching `packages/shared`'s existing convention — this is `apps/api`'s first colocated unit test). Booted the compiled server for real: `curl -i http://localhost:3000/health` → 200 OK, then killed it by its real PID (per DB0-07's Windows PID lesson).
- follow-up: hit two real issues. (1) Importing `@devbrain/shared` from `apps/api` for the first time reproduced DB0-08's exact `TS2307: Cannot find module '@devbrain/shared'` fresh-clone failure (reproduced on purpose by deleting `packages/shared/dist` first) — fixed with the identical remedy (`"references": [{"path": "../../packages/shared"}]` + `tsc -b` for the `typecheck` script). (2) A near-miss: tried adding `noEmit: true` to `apps/api/tsconfig.json` to match `apps/web`'s clean "tsc is just a checker" pattern — this silently broke `nest build` (**zero output, no error**, no `dist/main.js`), because unlike `web` (where Vite alone produces real output), `apps/api/tsconfig.json` is the *same file* `nest build` reads for the real server. Caught only by explicitly re-running `nest build` in isolation after the change; reverted. Confirmed the thing `noEmit` was meant to avoid (typecheck's `tsc -b` also writing to `dist/`) is harmless — `nest-cli.json`'s `deleteOutDir: true` means the real build always cleans and rewrites `dist/` afterward. Eighth learn-log report written: [docs/learn-log/DB1-02-shared-capture-dtos.md](../docs/learn-log/DB1-02-shared-capture-dtos.md). Next eligible: DB1-03 (`CapturesModule`: `POST /captures` + validation).
- commit: tasks(DB1-02): shared capture dtos

## 2026-07-18 — DB1-03 CapturesModule: POST /captures (+ validation)
- changed: DevBrain's first real feature. Added `class-validator`/`class-transformer` to `apps/api` and a global `ValidationPipe({whitelist:true, transform:true})` in `main.ts`. Added `apps/api/src/captures/{dto/create-capture.dto.ts,captures.service.ts,captures.controller.ts,captures.module.ts}` — `POST /captures` validates the body, creates a `Capture` row (`status` defaults to `'raw'` per the schema), and returns it shaped through `toCaptureDto` (the mapper written in DB1-02). Registered `CapturesModule` in `AppModule`. Appended DB1-03's detailed spec to `tasks/backlog/phase-1.md`.
- files: `apps/api/{package.json,src/main.ts,src/app.module.ts,src/captures/{dto/create-capture.dto.ts,captures.service.ts,captures.controller.ts,captures.module.ts},test/captures.e2e-spec.ts}`, `pnpm-lock.yaml` (generated), `tasks/backlog/phase-1.md`
- verify: `pnpm exec vitest run test/captures.e2e-spec.ts` → 4/4 passed (creates with `status=raw`; succeeds without `task`, returns `null`; missing `rawText` → 400; unknown `status` field stripped, never persisted). Full clean-state verification (deleted every `dist/`/`.tsbuildinfo` repo-wide, ran typecheck → lint → test → build in CI order) all green across all 4 packages. Confirmed no `.spec.js` leaked into `apps/api/dist/`. Booted the compiled server for real and drove actual `curl` requests: a valid `POST /captures` → 201 with the correct shape (`task:null` when omitted); an invalid one (missing `rawText`) → 400 with real `class-validator` messages. Killed the boot-test server by its real PID (per DB0-07's Windows PID lesson) and cleaned up the row it created in `dev.db`.
- follow-up: verified the e2e test's own cleanup actually works (not just trusted `afterAll`) — wrote a throwaway probe counting leftover `source='e2e-test'` rows after the suite ran (zero), then deleted the probe. Minor tooling snag along the way: tried inspecting `dev.db` directly via `better-sqlite3` from a Node script and hit `Cannot find module` — it's a *transitive* dependency (of `@prisma/adapter-better-sqlite3`), not a direct one, so pnpm's strict layout doesn't expose it to `apps/api`'s own scripts; used the already-direct `PrismaClient` import instead (same pattern as DB1-01's probe). Ninth learn-log report written: [docs/learn-log/DB1-03-captures-post-endpoint.md](../docs/learn-log/DB1-03-captures-post-endpoint.md). Next eligible: DB1-04 (`GET /captures?status=raw`, inbox, newest first).
- commit: tasks(DB1-03): captures module post endpoint

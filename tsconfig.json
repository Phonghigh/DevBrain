{
  // Solution-style root config. Holds no files of its own; packages register
  // themselves here (references) as they're scaffolded, so `tsc -b` builds the graph.
  "files": [],
  "references": [{ "path": "./packages/shared" }]
}
